server:
  port: ${PORT:8080}

spring:
  application:
    name: fantasy-scoring
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-bold-field-abnobptd-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require}
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASSWORD:npg_X3dnpsye1VOW}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: fantasy_league
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  hikari:
    maximum-pool-size: 5

apifootball:
  base-url: https://v3.football.api-sports.io
  api-key: ${API_FOOTBALL_KEY:0da66c440ae9302c51160e64fc562209}